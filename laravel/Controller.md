# Controller

```Linux
php artisan make:controller [Controller Name]
php artisan make:controller [Controller Name] --resource
```

- namespace
  - 透過namespace找到controller
- use
  - 使用物件
  - 引用物件才可以使用

- 當有新增檔案更動時，讓server重新跑起來會去讀新檔案

```Linux
composer dump-autoload
```

```Linux
php artisan route:list
```
